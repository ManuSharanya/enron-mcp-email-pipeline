# ── Secrets & credentials ─────────────────────────────────────────────────────
# API keys and override email — never commit
.env
# Google OAuth client secret — never commit
credentials.json
# OAuth access token written by mcp-gmail — never commit
token.json
# Alternative GCP key filename — never commit
gcp-oauth.keys.json

# ── MCP config (contains Gmail address) ──────────────────────────────────────
mcp_config.json

# ── Large data ────────────────────────────────────────────────────────────────
# Raw Enron maildir dataset (hundreds of MB)
enron_mail_20150507/
enron_mail_20150507.tar.gz
# SQLite database (generated by pipeline)
enron_emails.db
enron_emails.db-shm
enron_emails.db-wal

# ── Pipeline outputs (keep generated outputs for submission) ─────────────────
# output/replies/ and output/send_log.csv are required by the spec — tracked
# duplicates_report.csv and error_log.txt are required by the spec — tracked

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# ── Claude Code local settings ────────────────────────────────────────────────
.claude/

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/
del
